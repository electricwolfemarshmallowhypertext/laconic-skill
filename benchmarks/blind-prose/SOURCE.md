# Blind prose benchmark source

This corpus contains 100 assistant-message examples sampled from the public Hugging Face dataset `HuggingFaceH4/ultrachat_200k`, `default` config, `train_sft` split.

Dataset license metadata reported by Hugging Face: `mit`.

This corpus is used for same-input comparison reporting, not for tuning verifier pass/fail thresholds.
