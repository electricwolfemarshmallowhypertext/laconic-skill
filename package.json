{
  "name": "laconic-skill",
  "version": "0.1.0",
  "description": "Source-available deterministic concise-response skill and runtime",
  "license": "BUSL-1.1",
  "files": [
    "dist/",
    "skills/",
    "examples/",
    "README.md",
    "LICENSE",
    "package.json"
  ],
  "type": "commonjs",
  "main": "dist/index.js",
  "bin": {
    "laconic": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node test/laconic.test.js",
    "benchmark": "npm run build && tsc -p tsconfig.benchmark.json && node dist/scripts/benchmark.js"
  },
  "keywords": [
    "skill",
    "deterministic",
    "brevity",
    "cli"
  ],
  "devDependencies": {
    "@types/node": "^22.15.19",
    "typescript": "^5.8.3"
  }
}
